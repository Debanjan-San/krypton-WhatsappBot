module.exports.execute = async (client, flag, arg, M) => {
    M.reply('*Hwllo Human* 🤖')
}

module.exports.command = {
    name: 'alive',
    aliases: ['a'],
    category: 'music',
    usage: '',
    exp: 0,
    description: 'Testing stuff'
}
